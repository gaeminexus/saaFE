export interface EstadoParticipe {
    codigo: number;         // Código del estado partícipe
    nombre: string;         // Nombre del estado partícipe
    codigoExterno: number;  // Código externo
    idEstado: number;       // ID del estado
}
