export interface CreditoMontoAprobacion {
    codigo: number;        // Código del crédito
    estado: number;        // Estado del crédito
    idProceso: number;     // ID del proceso
    montoMaximo: number;   // Monto máximo aprobado
    montoMinimo: number;   // Monto mínimo aprobado
}
