import { LiquidacionCompraCompra } from './liquidacion-compra-compra';

// Ruta del documento generado para LiquidacionCompraCompra — endpoint: /plcc
export interface PathLiquidacionCompraCompra {
  id: number;
  liquidacion: LiquidacionCompraCompra;
  path: string;
  alterno: number;
}
